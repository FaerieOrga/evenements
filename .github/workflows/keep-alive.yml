name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 8 * * *'  # Tous les jours à 8h UTC
  workflow_dispatch:       # Permet de le lancer manuellement

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s "https://zutgjwnmolzwkbkdzfko.supabase.co/rest/v1/evenements?select=id&limit=1" \
          -H "apikey: sb_publishable_E_81QRnHPOXTDcxLv9l2wQ_dpqspXFf" \
          -H "Authorization: Bearer sb_publishable_E_81QRnHPOXTDcxLv9l2wQ_dpqspXFf"
          echo "Ping effectué"
