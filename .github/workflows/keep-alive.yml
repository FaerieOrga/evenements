name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 8 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        env:
          SUPABASE_KEY: sb_publishable_E_81QRnHPOXTDcxLv9l2wQ_dpqspXFf
        run: |
          curl -s "https://zutgjwnmolzwkbkdzfko.supabase.co/rest/v1/evenements?select=id&limit=1" \
            -H "apikey: $SUPABASE_KEY" \
            -H "Authorization: Bearer $SUPABASE_KEY"
          echo "Ping OK"
